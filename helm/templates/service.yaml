apiVersion: v1
kind: Service
metadata:
  name: {{ include "mestier-website.fullname" . }}
  labels:
    {{- include "mestier-website.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mestier-website.selectorLabels" . | nindent 4 }}
