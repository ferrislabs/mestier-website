apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mestier-website.fullname" . }}-test-connection"
  labels:
    {{- include "mestier-website.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mestier-website.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
